import { Vec2 } from './protocol/shared'
import { GameObject } from './game_objects/game_object'

// export function canPickObject(pos: Vec2, object: GameObject) {
//     return typeof object.pos !== 'string' && object.pos === pos
// }
